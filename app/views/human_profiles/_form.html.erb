<%= form_for(@human_profile) do |f| %>
  <% if @human_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@human_profile.errors.count, "error") %> prohibited this human_profile from being saved:</h2>

      <ul>
      <% @human_profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
